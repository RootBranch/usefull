#pragma once
#pragma once
#include<string>
#include "Fraction.h"
using namespace std;
fraction isDecimal(string, int);
fraction judge(string);
fraction isFraction(string, int);
fraction isNumber(string);